/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.dummyjson.com",
      "images.unsplash.com",
      "plus.unsplash.com",
      "placehold.co"
    ],
  },
};

module.exports = nextConfig;